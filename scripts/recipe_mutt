#!/usr/bin/env janet
# vim: set ft=janet

(def mutt_dir
  (string/format "%s/.config/mutt" (os/getenv "HOME")))
(def mailcap "text/html; w3m -I %%{charset} -T text/html; copiousoutput;\n")
(os/mkdir mutt_dir)
(spit (string/format "%s/mailcap" mutt_dir) mailcap)
